#include "guiplayervertical.h"

GUIPlayerVertical::GUIPlayerVertical( Players* p1, Players* p2, Players* p3, Players* p4 ) : GUIPlayer( p1,  p2,  p3, p4  ) {
  /*diceButton = new QPushButton( this );
  diceButton->setIcon( *dice );
  diceButton->setIconSize( QSize(5, 5) );
  diceButton->setGeometry( 1, 1, 5, 5);*/
}
